import "./QuizType.scss";

interface Props {
    title: string,
    
};

const QuizType = (props: Props) : JSX.Element => {
    return (
        <div className = "quiz-type">
            
        </div>
    );
};

export default QuizType;